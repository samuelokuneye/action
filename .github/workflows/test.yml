name: test
on: push
jobs:
  run-shell-commands:
    runs-on: ubuntu-latest
    steps:
      - name: checkout
        uses: actions/checkout@v3
      - name: present workdir
        run: pwd
      - name: echo something
        run: echo "Jesus is Lord"

